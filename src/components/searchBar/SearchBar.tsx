import "./SearchBar.css";

const SearchBar = () => {
  return (
    <div className="searchbar-container">
      <div className="searchbar">
        <div className="searchbar-text">Search for a location...</div>
      </div>
    </div>
  );
};

export default SearchBar;
